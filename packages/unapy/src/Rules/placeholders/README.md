# Future Rule Sets Placeholder

Reserved for upcoming server-side rule implementations:

- `stacking-chaos`
- `jump-in`
- `seven-zero`

Each rule set should expose isolated validation/effect logic and be selected per game instance.
