# Basic Rule Set

This folder contains server-side hooks for the `basic` rule set.

At the moment, gameplay logic is still handled directly in `GameService`.
Keep future rule-specific logic in this folder and route through `GameRulesService`.
